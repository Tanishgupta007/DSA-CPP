#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    // int n1,n2;
    // cout<<"n1: "; 
    // cin >> n1;
    // cout<<"n2: "; 
    // cin >> n2;

    // int digits = (int)log10(n2) + 1;

    // int res = n1*pow(10,digits) + n2;

    // cout<<res<<endl; //we are getting res-1 because of the compiler

    // //so to print correct answer we need to add 1 to the result 
    // cout<<res+1<<endl;
    int value = 1;
    switch (value)
    {
    case 1: cout<<"One";
    case 2:cout<<"Two";

 
    }


}